export interface Category {
    title: string;
    description: string;
    href: string;
    bgColor: string;
    hoverColor: string;
}

export interface FilterOption {
    value: string;
    label: string;
}